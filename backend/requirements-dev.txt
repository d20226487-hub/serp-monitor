# Test-only dependencies. Kept out of requirements.txt so the runtime image
# stays lean — CI installs both, the Dockerfile installs only the runtime set.
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.0
